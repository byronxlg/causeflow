import type { GenerateResponse } from '@/lib/types';

interface ToolbarProps {
  data: GenerateResponse;
}

export function Toolbar({}: ToolbarProps) {
  return null; // Toolbar removed - export/share functionality disabled
}